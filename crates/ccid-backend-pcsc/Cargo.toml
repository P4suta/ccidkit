# SPDX-FileCopyrightText: 2026 ccidkit contributors
#
# SPDX-License-Identifier: MIT OR Apache-2.0

[package]
name = "ccid-backend-pcsc"
description = "PC/SC shim backend: the platform smart card service behind the ccid-core traits"
keywords = ["pcsc", "smartcard", "winscard", "ccid", "reader"]
categories = ["hardware-support", "os", "authentication"]
readme = "../../README.md"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true

# Dependencies are declared when code needs them: the `pcsc` crate arrives with the
# first context (M3), and the workspace edges may only be core and apdu (`just deps`).
[dependencies]

[lints]
workspace = true
