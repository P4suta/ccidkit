# SPDX-FileCopyrightText: 2026 ccidkit contributors
#
# SPDX-License-Identifier: MIT OR Apache-2.0

[package]
name = "ccid-core"
description = "Backend, reader, card, and transaction traits for pure-Rust smart card access"
keywords = ["smartcard", "ccid", "reader", "iso7816", "pcsc"]
categories = ["hardware-support", "api-bindings", "authentication"]
readme = "../../README.md"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true

# Dependencies are declared when code needs them. The only edge this crate may ever
# declare is `ccid-apdu` (docs/adr/0013), enforced by `just deps` and `just purity`.
[dependencies]

[lints]
workspace = true
